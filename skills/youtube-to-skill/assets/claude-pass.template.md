# Claude pass  (Step 5 -- frame + transcript reading)

<!--
  HOW THIS FILE GETS FILLED (do it on your Mac, where /watch works):

    /watch <YOUTUBE_URL> --no-whisper [--detail transcript | --resolution 1024] [--start .. --end ..]

  Then read the extracted frames + captions and write this file BEFORE you ever
  look at notes/gemini-pass.md. Order matters: writing Claude's reading first
  keeps it from anchoring to Gemini's second opinion.

  Rules for both passes:
    * Report only what is actually on screen or said. "not shown" beats a guess.
    * Named things (commands, tool names, exact strings, paths, error messages,
      config keys/values) are the reliable part -- capture them verbatim.
    * Numbers (durations, counts, timestamps) are decoration -- treat as approx.
-->

- URL:
- Window: full  (or START -> END)
- /watch flags used:
- Question I need answered:

## 1. What it teaches

## 2. Step-by-step build (as demonstrated)

## 3. Exact commands / config / strings shown on screen
<!-- quote verbatim in `code spans` / code blocks -->

## 4. Assumed tools / dependencies (never installed on screen)

## 5. Stated prerequisites / asides ("you'll want to have already done X")
<!-- often the single most valuable lines in the whole video -->

## 6. Unclear / ambiguous / possibly presenter-specific
